---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: classificationrules.autolabeller.autolabeller.github.com
spec:
  group: autolabeller.autolabeller.github.com
  names:
    kind: ClassificationRule
    listKind: ClassificationRuleList
    plural: classificationrules
    singular: classificationrule
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClassificationRule is the Schema for the classificationrules
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClassificationRule
            properties:
              conflictPolicy:
                default: Merge
                description: ConflictPolicy defines the policy to apply when there
                  is a conflict in labeling
                enum:
                - Overwrite
                - Merge
                - Ignore
                - Error
                type: string
              labels:
                additionalProperties:
                  type: string
                description: |-
                  Labels defines the labels to apply when a resource matches the rule
                  Key = label name
                  Value = label value
                type: object
              match:
                description: |-
                  Match defines the resource fields to match for labelling
                  Key = field name (e.g., "image", "name", "namespace")
                  Value = expected value to match (e.g., "nginx", "prod_proxy", "production")
                properties:
                  commonMatch:
                    description: Common criteria applicable to all resource types
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations is a map of annotation keys and values
                          to match
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels is a map of label keys and values to match
                        type: object
                      name:
                        description: Name is the resource name to match. Supports
                          wildcard patterns (* and ?).
                        type: string
                      namespace:
                        description: Namespace is the namespace name to match. Exact
                          string match.
                        type: string
                    type: object
                  deploymentMatch:
                    description: Deployment-specific match criteria
                    properties:
                      imagePullPolicy:
                        description: |-
                          ImagePullPolicy matches Deployments with specific image pull policy.
                          Valid values: Always, Never, IfNotPresent
                        type: string
                      replicas:
                        description: |-
                          Replicas matches Deployments with specific replica count.
                          Supports comparison operators (e.g., ">3", "==5").
                        type: string
                      strategy:
                        description: |-
                          Strategy matches Deployments with specific update strategy.
                          Valid values: RollingUpdate, Recreate
                        type: string
                    type: object
                  nodeMatch:
                    description: Node-specific match criteria
                    properties:
                      archLabels:
                        description: |-
                          ArchLabels matches nodes with specific architecture labels.
                          Valid values: amd64, arm64, arm, ppc64le, s390x
                        items:
                          type: string
                        type: array
                      containerRuntime:
                        description: |-
                          ContainerRuntime matches nodes with specific container runtime.
                          Examples: docker, containerd, cri-o
                        type: string
                      kernelVersion:
                        description: |-
                          KernelVersion matches nodes with kernel versions matching the pattern.
                          Supports comparison operators.
                        type: string
                      osLabels:
                        description: |-
                          OSLabels matches nodes with specific OS labels.
                          Valid values: linux, windows
                        items:
                          type: string
                        type: array
                      taints:
                        description: |-
                          Taints matches nodes with specific taints.
                          Each taint is matched as key=value:effect format.
                        items:
                          type: string
                        type: array
                    type: object
                  podMatch:
                    description: Pod-specific match criteria
                    properties:
                      cpuLimits:
                        description: |-
                          CPULimits matches Pods with CPU limits matching the specified value.
                          Supports comparison operators.
                        type: string
                      cpuRequests:
                        description: |-
                          CPURequests matches Pods with CPU requests matching the specified value.
                          Supports comparison operators (e.g., ">1", "<=500m").
                        type: string
                      hostNetwork:
                        description: HostNetwork matches Pods with hostNetwork setting.
                        type: boolean
                      images:
                        description: |-
                          Images is a list of container image patterns to match.
                          Each pattern supports wildcard matching (* and ?).
                        items:
                          type: string
                        type: array
                      memoryLimits:
                        description: |-
                          MemoryLimits matches Pods with memory limits matching the specified value.
                          Supports comparison operators.
                        type: string
                      memoryRequests:
                        description: |-
                          MemoryRequests matches Pods with memory requests matching the specified value.
                          Supports comparison operators (e.g., ">1Gi", "<=512Mi").
                        type: string
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: NodeSelector is a map of node labels to match
                          for Pod scheduling.
                        type: object
                      restartPolicy:
                        description: |-
                          RestartPolicy matches Pods with specific restart policy.
                          Valid values: Always, OnFailure, Never
                        type: string
                      serviceAccount:
                        description: ServiceAccount is the name of the ServiceAccount
                          to match. Exact match.
                        type: string
                    type: object
                type: object
              refreshInterval:
                default: 30s
                description: |-
                  RefreshInterval defines how often the rule should be re-evaluated and reapplied.
                  Must be a valid duration string (e.g., "30s", "5m", "1h").
                  Defaults to 30s if not specified.
                type: string
              suspend:
                default: false
                description: Suspend temporarily disables the application of this
                  classification rule
                type: boolean
              targetKind:
                default: Pod
                description: TargetKind specifies the Kubernetes resource type to
                  apply the rule to
                enum:
                - Pod
                - Node
                - Namespace
                - Service
                - Deployment
                - StatefulSet
                - DaemonSet
                - ReplicaSet
                - Job
                - CronJob
                type: string
            required:
            - targetKind
            type: object
            x-kubernetes-validations:
            - message: commonMatch.namespace must be empty when targetKind is Node
              rule: self.targetKind != 'Node' || self.match == null || self.match.commonMatch
                == null || self.match.commonMatch.namespace == ""
          status:
            description: status defines the observed state of ClassificationRule
            properties:
              MatchedResourceCount:
                description: matchedResourcesCount indicates the number of resources
                  that matched this rule.
                format: int32
                type: integer
              conditions:
                description: The status of each condition is one of True, False, or
                  Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastError:
                description: lastError provides details of the last error encountered
                  while applying the rule.
                type: string
              lastReconciled:
                description: lastAppliedTime indicates the last time the rule was
                  successfully applied.
                format: date-time
                type: string
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed for this ClassificationRule.
                  It corresponds to the ClassificationRule's generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
